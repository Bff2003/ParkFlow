{
  "name": "parkflow-backend",
  "version": "1.0.0",
  "description": "1. Install [Node.js](https://nodejs.org/en/download/)\r 2. run `npm install` in the backend directory\r 3. run `npm start` to start the server\r 4. the server will be running on port setting in the .env file",
  "main": "index.ts",
  "scripts": {
    "start": "npx nodemon src/index.ts",
    "build": "npx tsc",
    "build:run": "npm run build && node build/index.js",
    "lint": "npx eslint . --ext .ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bff2003/ParkFlow.git"
  },
  "author": "Bernardo Farrobinha",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Bff2003/ParkFlow/issues"
  },
  "homepage": "https://github.com/Bff2003/ParkFlow#readme",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.1",
    "reflect-metadata": "^0.1.13",
    "sqlite3": "^5.1.6",
    "ts-node": "^10.9.1",
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.1"
  }
}
